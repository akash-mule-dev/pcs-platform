// The AR scene was replaced by the richer glb-viewer port in ./ar (solid/ghost/
// wireframe render modes, world/plane/image tracking, rulers + dimensions).
// Re-exported here so any existing import path keeps resolving.
export { default } from './ar/ARModelScene';
