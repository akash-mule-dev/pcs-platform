{
  "buildCommand": "node stamp-build.js && if [ \"$VERCEL_GIT_COMMIT_REF\" = \"dev\" ]; then npm run build -- --configuration dev; else npm run build -- --configuration production; fi",
  "outputDirectory": "dist/frontend/browser",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
