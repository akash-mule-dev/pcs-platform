export const environment = {
  production: false,
  // Dev environment -> dev backend (Vercel stable git-branch URL), backed by pcs-dev-db.
  // Switch to a custom domain (e.g. https://demo-api.fabrixr.com/api) once DNS is set.
  apiUrl: 'https://backend-git-dev-akash-mule-devs-projects.vercel.app/api'
};
